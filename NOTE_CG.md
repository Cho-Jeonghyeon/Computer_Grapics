# 컴퓨터 그래픽스 프로젝트 메모

## 사용자 선호

- 대화는 한국어를 선호한다.
- 설명은 문법만 나열하기보다 코드 구조와 직관을 함께 연결해서 설명한다.
- 사용자는 Visual Studio에서 C++와 컴퓨터 그래픽스 실습 과제를 진행 중이다.
- 개념 질문에는 왜 그렇게 설계하는지까지 설명한다.
- 직접 구현 요청이 있으면 가능한 경우 수정 후 빌드/실행으로 확인한다.

## 프로젝트 정보

- 작업 경로: `C:\Users\USER\source\repos\computer_grapics`
- GitHub 저장소: `https://github.com/Cho-Jeonghyeon/Computer_Grapics`
- 기본 브랜치: `main`
- 솔루션 파일: `computer_grapics.slnx`
- 자동화에서 사용한 Visual Studio 경로: `C:\Program Files\Microsoft Visual Studio\18\Community\Common7\IDE\devenv.exe`
- 자동화에서 사용한 Chrome 경로: `C:\Program Files\Google\Chrome\Application\chrome.exe`

## 응답 스타일

- Codex 답변은 하늘색 단순 구분선으로 시작하고 끝낸다.
- 작업 중간 업데이트도 별도 제목 없이 같은 시작/끝 구분선만 사용한다.
- 시작 구분선: `━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Codex ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`
- 끝 구분선: `━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`
- 가능하면 ANSI 하늘색 코드 `\x1b[96m...\x1b[0m`를 사용한다.

## 로컬 자동화

- 사용자 명령 문구: `컴그 시작하자`
- 노트북과 데스크톱은 창 배치가 다르므로 서로 덮어쓰지 않는다.
- 데스크톱 설정은 2026-09-06에 갱신됨.
- 데스크톱 저장소 경로: `C:\Users\user\source\repos\Computer_Grapics\Computer_Grapics`
- 데스크톱 진입 스크립트: `C:\Users\user\start-comg.ps1`
- Git 추적 자동화 스크립트: `scripts\start-comg-desktop.ps1`
- 데스크톱에서 `컴그 시작하자`라고 하면 해당 스크립트로 앱을 열고 배치한다.
- 기본 배치는 왼쪽 ChatGPT/YouTube Music, 가운데 PowerShell 또는 Windows Terminal, 오른쪽 Visual Studio이다.
- 기존 창이 있으면 중복 실행하지 않고 가능한 한 유지한다.
- 여러 터미널 창이 있으면 `-TerminalWindowHandle`로 의도한 창을 지정한다.
- `-Inspect`는 창을 실행하거나 이동하지 않고 현재 창 상태만 보고한다.
- 이전 노트북 설정은 ChatGPT 왼쪽 절반, Visual Studio 오른쪽 절반, YouTube Music 최대화 배치이다.

## 워밍업 1 과제 매핑

- 과제 PDF: `docs\컴그 워밍업 1.pdf`
- `Lab_01`: 문제 1, 4x4 행렬 프로그램.
- `Lab_02`: 문제 2, 파일/문자열 처리.
- `Lab_03`: 문제 3, 구조체를 사용한 점 리스트.
- `Lab_04`: 문제 4, 카드 매칭 게임.

## Lab_01 상태

- `Lab_01` 문제 1은 완료 및 커밋됨.
- 커밋: `57430d8 실습 1 마무리`
- 구현된 명령: `m`, `a`, `d`, `r`, `t`, `e`, `f`, `+`, `-`, `s`, `q`.
- `baseMatrix1`, `baseMatrix2`는 원본 기준 행렬이고, `matrix1`, `matrix2`는 현재 표시/연산용 행렬이다.
- `rowMinMode`, `colMaxMode`는 토글 상태를 저장한다.
- `UpdateCurrentMatrix()`는 원본 행렬과 활성 모드를 기준으로 현재 행렬을 다시 만든다.

## Lab_02 상태

- `Lab_02` 문제 2는 진행 중이다.
- 최근 구현 커밋: `88ecd11 Lab02 문자열 명령 b부터 f까지 구현`
- 과제 샘플 텍스트는 `c2bf381 Lab02 과제 입력 데이터 갱신` 커밋에서 `Lab_02/data.txt`에 추가됨.
- 구현된 동작:
  - 파일 이름을 입력받고 모든 줄을 `vector<string> sentences`에 읽어온다.
  - `a`: 전체 문장의 대소문자 변환 출력을 토글한다.
  - `b`: 각 문장과 단어 수를 출력한다.
  - `c`: 첫 글자가 대문자인 단어를 강조하고 총 개수를 출력한다.
  - `d`: 각 문장 전체를 문자 단위로 뒤집는다.
  - `e`: 모든 공백을 `*`로 표시한다.
  - `f`: 단어 순서는 유지하고 각 단어 내부 문자만 뒤집는다.
  - `q`: 종료한다.
- 토글 변수는 `main()`에 둬서 함수 호출 후에도 상태가 유지되게 한다.
- `Lab_02` Debug x64 빌드는 오류 0개로 성공했고, C4267 경고 3개가 남아 있다.
- 남은 과제 명령: `g`, `h`, `i`, `j`.

## Lab_02 개념 메모

- `vector<string>`은 여러 줄을 저장하며, 각 원소가 한 문장/한 줄이다.
- `sentences.size()`는 저장된 줄 수이다.
- `sentences[i].length()`는 `i`번째 줄의 문자 수이다.
- `const vector<string>& sentences`는 벡터를 복사하지 않고 참조로 넘기며, 함수 안에서 수정하지 못하게 한다.
- `islower`, `isupper`, `toupper`, `tolower`는 `<cctype>`에 있다.
- 단어 수 계산은 공백 전환을 기준으로 한다.
- 여러 공백은 하나의 구분자로 취급한다.
- `/`, `-`, `*`, punctuation 같은 특수문자는 공백으로 분리되지 않는 한 단어를 나누지 않는다.

## Git 상태 메모

- 최근 주요 커밋:
  - `88ecd11 Lab02 문자열 명령 b부터 f까지 구현`
  - `c2bf381 Lab02 과제 입력 데이터 갱신`
  - `cd75750 데스크탑 컴그 시작 자동화 추가`
  - `a223595 AI 노트 최신화`
  - `bf0fa4b Lab02 대소문자 토글 및 출력`
  - `57430d8 실습 1 마무리`
  - `385cb12 Lab 01 기본 명령어와 작업 기록 추가`
  - `0fb2a6d 워밍업 1 세팅`
  - `05d6647 Initial project setup`
- 데스크톱 저장소의 Git identity는 기존 이력과 동일하다: `Cho-Jeonghyeon <69703120+Cho-Jeonghyeon@users.noreply.github.com>`

## 2026-09-08 작업 메모

- 사용자는 알려진 버그가 남아 있어도 현재 진행 상황을 커밋하고 push해달라고 요청했다.
- 알려진 `Lab_01` 이슈: `e` / `f` 토글과 `+` / `-` 조합 주변 버그가 있다.
- `Lab_02` 진행 상황:
  - `g`: 요청한 문자를 다른 문자로 바꾼다.
  - `h`: 숫자를 찾고, 숫자 뒤 텍스트를 다음 줄로 이동한다.
  - `i`: 검색 단어를 입력받아 일치 단어를 색으로 표시하고, 대소문자 구분 없이 개수를 센다.
  - `j`: 처음에는 loop index 버그가 있었고, 2026-09-09 업데이트에서 해결됨.

## 2026-09-09 데스크톱 인계

- `Lab_01`을 검토했고 오류 0개로 빌드 성공했다.
- `Lab_01`은 `e`, `f`, `+`, `-` 단독 동작은 정상이나, `e` 또는 `f`가 활성화된 상태에서 `+` / `-`를 누르는 조합 버그는 남아 있다.
- `Lab_02`의 `Command_j`는 순환 하향 이동으로 수정됐다.
- `Lab_02` Debug x64 빌드는 기존 C4267 경고 3개만 남기고 성공한다.
- `Lab_03`은 `Lab_03/main.cpp`에 deque 기반 미완성 초안으로 시작됐고, 이후 워밍업 완료 작업에서 마무리됐다.

## 2026-09-09 워밍업 완료

- 사용자는 워밍업 과제 기간이 지났고, 완료된 작업을 하나의 큰 커밋이 아니라 여러 커밋으로 나누길 원했다.
- `Lab_03`은 의도적으로 단순한 학생 스타일의 C++ 구현으로 완료됨.
- `Lab_03`은 `Point { x, y, z }`, 고정 배열, `bool used[10]`, 단순 반복문을 사용한다.
- `Lab_03` 주요 명령: `+`, `-`, `e`, `d`, `a`, `b`, `c`, `f`, `g`.
- `Lab_04`는 단순 콘솔 카드 매칭 게임으로 구현됨.
- `Lab_04`는 전역 `board[6][6]`, `opened[6][6]` 배열을 사용한다.
- `Lab_04`는 `r` reset, `h` 임시 전체 공개, `q` 종료를 지원한다.
- `Lab_03` Debug x64 빌드는 경고 0개, 오류 0개로 성공했다.
- `Lab_04` Debug x64 빌드는 경고 0개, 오류 0개로 성공했다.
